window.addEventListener("DOMContentLoaded", () => {
  const THEME_STORAGE_KEY = "filtroSorteioProcessual.theme.v1";
  const REQUIRED = ["Cliente","Número de CNJ","Tipo","Valor da causa","Última Decisão","Critério"];
  const DEFAULT_COLS = [...REQUIRED,"Sócio a gerir","Sorteado Para"];
  const RANDOM_GROUPS = ["EF","IMPROCEDENTE","ED","EP","ALEATORIO"];
  const FIXED_GROUPS = ["ANA","FLAVIO","NADJA/FLAVIO","NADJA","COMPROMETIDO"];
  const PRIORITY = ["EF",...FIXED_GROUPS,"IMPROCEDENTE","ED","EP","ALEATORIO"];
  let master=[], filtered=[], available=[], selected=[...DEFAULT_COLS], activeFilter="", batchDone=false, groupMap=new Map();
  const $=id=>document.getElementById(id);
  const norm=v=>String(v??"").normalize("NFD").replace(/\p{Diacritic}/gu,"").toUpperCase().replace(/\s+/g," ").trim();
  const isNumericId=v=>/^\d+$/.test(String(v??"").trim());
  const isCnj=v=>/^\d{7}-\d{2}\.\d{4}\.\d\.\d{2}\.\d{4}$/.test(String(v??"").trim());
  const isProcessRow=r=>String(r?.Cliente??"").trim()&&(isNumericId(r.ID)||isNumericId(r.ID1)||isCnj(r["Número de CNJ"]));
  const key=r=>isNumericId(r.ID)?`ID:${String(r.ID).trim()}`:isNumericId(r.ID1)?`ID1:${String(r.ID1).trim()}`:isCnj(r["Número de CNJ"])?`CNJ:${String(r["Número de CNJ"]).trim()}`:`R:${norm(r.Cliente)}|${norm(r.Pasta)}`;
  const parseBRL=value=>{if(typeof value==="number")return Number.isFinite(value)?value:0;let t=String(value??"").trim(),neg=/^-|^-\s*R\$|^\(.*\)$/i.test(t);if(/^\(.*\)$/.test(t))t=t.slice(1,-1);t=t.replace(/R\$/gi,"").replace(/-/g,"").replace(/\s/g,"");if(t.includes(","))t=t.replace(/\./g,"").replace(/,/g,".");const n=Number.parseFloat(t);return Number.isFinite(n)?(neg?-n:n):0;};
  const brl=v=>parseBRL(v).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
  const pct=(a,b)=>b?`${((a/b)*100).toFixed(2)}%`:"0%";
  const partner=r=>{const v=norm(r["Sorteado Para"]);return v.includes("ANA")?"Ana":v.includes("FLAVIO")?"Flávio":"";};

  function ensureModal(){if($("fspModal"))return;const box=document.createElement("div");box.id="fspModal";box.hidden=true;box.innerHTML=`<div class="fsp-modal-backdrop"></div><section class="fsp-modal-card" role="dialog" aria-modal="true" aria-labelledby="fspModalTitle"><header><h2 id="fspModalTitle"></h2><button id="fspModalX" type="button" aria-label="Fechar">×</button></header><div id="fspModalBody"></div><footer><button id="fspModalCancel" class="btn btn-neutral" type="button">Fechar</button><button id="fspModalConfirm" class="btn btn-sorteio" type="button" hidden>Confirmar</button></footer></section>`;document.body.appendChild(box);const style=document.createElement("style");style.textContent=`#fspModal{position:fixed;inset:0;z-index:1500;display:grid;place-items:center;padding:16px}#fspModal[hidden]{display:none}.fsp-modal-backdrop{position:absolute;inset:0;background:rgba(15,23,42,.74)}.fsp-modal-card{position:relative;width:min(760px,100%);max-height:90vh;overflow:auto;background:var(--surface);color:var(--text);border:1px solid var(--border);border-radius:12px;box-shadow:0 24px 70px rgba(0,0,0,.35)}.fsp-modal-card header,.fsp-modal-card footer{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:16px 20px;background:var(--summary-header-bg)}.fsp-modal-card header{border-bottom:1px solid var(--border)}.fsp-modal-card footer{border-top:1px solid var(--border);justify-content:flex-end}.fsp-modal-card header button{border:0;background:transparent;color:var(--text);font-size:28px;cursor:pointer}.fsp-modal-card #fspModalBody{padding:20px;line-height:1.55}.fsp-modal-card table{width:100%;margin-top:10px}.fsp-modal-card th,.fsp-modal-card td{padding:8px;border:1px solid var(--border);font-size:13px}`;document.head.appendChild(style);}
  function modal(title,html,{confirmText="",onConfirm=null}={}){ensureModal();const m=$("fspModal"),c=$("fspModalConfirm");$("fspModalTitle").textContent=title;$("fspModalBody").innerHTML=html;c.hidden=!confirmText;c.textContent=confirmText||"Confirmar";m.hidden=false;document.body.classList.add("modal-open");const close=()=>{m.hidden=true;document.body.classList.remove("modal-open");};$("fspModalX").onclick=close;$("fspModalCancel").onclick=close;m.querySelector(".fsp-modal-backdrop").onclick=close;c.onclick=()=>{close();if(onConfirm)onConfirm();};}

  function setTheme(t){const d=t==="dark";document.documentElement.dataset.theme=d?"dark":"light";$("themeToggle").setAttribute("aria-pressed",String(d));$("themeLabel").textContent=d?"Modo claro":"Modo escuro";}
  setTheme(localStorage.getItem(THEME_STORAGE_KEY)||(window.matchMedia?.("(prefers-color-scheme: dark)").matches?"dark":"light"));
  $("themeToggle").onclick=()=>{const n=document.documentElement.dataset.theme==="dark"?"light":"dark";setTheme(n);localStorage.setItem(THEME_STORAGE_KEY,n);};

  const tips=$("tipsDialog");const closeTips=()=>{tips.hidden=true;document.body.classList.remove("modal-open");};$("tipsBtn").onclick=()=>{tips.hidden=false;document.body.classList.add("modal-open");};$("tipsCloseBtn").onclick=closeTips;$("tipsCloseFooterBtn").onclick=closeTips;tips.addEventListener("click",e=>{if(e.target.hasAttribute("data-close-tips"))closeTips();});
  const tc=tips.querySelector(".tips-content");if(tc)tc.innerHTML=`<p class="tips-intro">O App trabalha com a planilha recebida do Jurídico sem usar os nomes antigos dos sócios para o sorteio.</p><section class="tips-step"><h3><span>1</span> Importe e escolha as colunas</h3><ol><li>Faça upload do .xlsx completo.</li><li>Escolha na tela as colunas que deseja exibir.</li><li><strong>Sorteado Para</strong> é criada automaticamente pelo App e começa vazia.</li></ol></section><section class="tips-step"><h3><span>2</span> Formação dos grupos</h3><ol><li>Cada processo entra em um único grupo operacional.</li><li>São tratados os grupos especiais e depois EF, Improcedente, ED, EP e Aleatório.</li><li>Aleatório recebe somente o restante que não pertenceu aos filtros anteriores.</li></ol></section><section class="tips-step"><h3><span>3</span> Regra 60/40</h3><ol><li>O 60/40 é calculado separadamente em cada grupo sorteável.</li><li>A quantidade para Flávio usa <strong>Math.round(total × 0,60)</strong>; Ana recebe o restante.</li><li>A quantidade é determinística. Apenas os nomes sorteados mudam a cada execução.</li></ol></section>`;

  const controls=document.querySelector(".controls"),colBtn=document.createElement("button"),previewBtn=document.createElement("button"),packageBtn=document.createElement("button");
  colBtn.className="btn btn-roxo";colBtn.type="button";colBtn.textContent="Escolher Colunas";colBtn.disabled=true;previewBtn.className="btn btn-amarelo";previewBtn.type="button";previewBtn.textContent="Conferir Grupos";previewBtn.disabled=true;packageBtn.className="btn btn-azul";packageBtn.type="button";packageBtn.textContent="Baixar Relatórios do Lote";packageBtn.disabled=true;controls.insertBefore(colBtn,$("applyFilterBtn"));controls.insertBefore(previewBtn,$("applyFilterBtn"));controls.appendChild(packageBtn);$("drawBtn").textContent="Executar Todos os Sorteios";
  if($("filterList")&&![...$("filterList").options].some(o=>norm(o.value)==="ALEATORIO")){const o=document.createElement("option");o.value="Aleatório";$("filterList").appendChild(o);}

  function mergeSheets(items){const map=new Map();for(const {name,rows} of items)for(const src of rows){const k=key(src);if(!map.has(k))map.set(k,{...src,__sheets:new Set()});const t=map.get(k);for(const c of Object.keys(src))if((t[c]===""||t[c]==null)&&src[c]!==""&&src[c]!=null)t[c]=src[c];t.__sheets.add(name);}return[...map.values()].map(r=>({...r,__sheets:[...r.__sheets]}));}
  function detectCols(items){const out=[],seen=new Set();for(const {rows} of items)for(const r of rows.slice(0,30))for(const c of Object.keys(r))if(!c.startsWith("__")&&!seen.has(c)){seen.add(c);out.push(c);}if(!seen.has("Sorteado Para"))out.push("Sorteado Para");return out;}
  function rawMatch(r,g){const type=norm(r.Tipo),dec=norm(r["Última Decisão"]),crit=norm(r.Critério),s=(r.__sheets||[]).map(norm);if(g==="EF")return type==="EF"||s.includes("EF");if(g==="ED")return type==="ED"||s.includes("ED");if(g==="EP")return type==="EP"||s.includes("EP");if(g==="IMPROCEDENTE")return dec==="IMPROCEDENTE"||s.includes("IMPROCEDENTE");if(g==="NADJA/FLAVIO")return crit.includes("NADJA - SOLICITOU FICAR FLAVIO")||s.some(x=>x.includes("NADJA - SOLICITOU FICAR FLAVIO"));if(g==="NADJA")return crit==="NADJA"||s.includes("NADJA");if(g==="COMPROMETIDO")return crit.includes("COMPROMET")||crit.includes("COMPREMET")||s.some(x=>x.includes("COMPROMET"));if(g==="FLAVIO")return crit.includes("CLIENTE FLAVIO")||s.some(x=>x.includes("CLIENTES FLAVIO"));if(g==="ANA")return crit.includes("CLIENTE ANA PAULA")||s.some(x=>x.includes("CLIENTE ANA PAULA"));if(g==="ALEATORIO")return crit.includes("ALEATORIO");return false;}
  function buildGroups(){groupMap=new Map(PRIORITY.map(g=>[g,[]]));for(const r of master){const g=PRIORITY.find(x=>rawMatch(r,x))||"";r.__group=g;if(g)groupMap.get(g).push(r);}}
  const groupRows=g=>groupMap.get(g)||[];
  const clearAssignments=()=>master.forEach(r=>r["Sorteado Para"]="");
  function assignFixed(){groupRows("ANA").forEach(r=>r["Sorteado Para"]="Ana");groupRows("NADJA").forEach(r=>r["Sorteado Para"]="Ana");["FLAVIO","NADJA/FLAVIO","COMPROMETIDO"].forEach(g=>groupRows(g).forEach(r=>r["Sorteado Para"]="Flávio"));}
  function rnd(max){if(max<=1)return 0;const a=new Uint32Array(1),lim=Math.floor(0x100000000/max)*max;let v;do{crypto.getRandomValues(a);v=a[0];}while(v>=lim);return v%max;}
  function shuffle(a){const b=[...a];for(let i=b.length-1;i>0;i--){const j=rnd(i+1);[b[i],b[j]]=[b[j],b[i]];}return b;}
  function quota(rows){const f=Math.round(rows.length*.60);return{flavio:f,ana:rows.length-f};}
  function assignRandom(g){const rows=groupRows(g),q=quota(rows);shuffle(rows).forEach((r,i)=>r["Sorteado Para"]=i<q.flavio?"Flávio":"Ana");return q;}

  $("excelFile").onchange=e=>{const file=e.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=ev=>{try{const wb=XLSX.read(new Uint8Array(ev.target.result),{type:"array",cellStyles:true}),items=[];let ignored=0;for(const name of wb.SheetNames){const all=XLSX.utils.sheet_to_json(wb.Sheets[name],{defval:"",raw:true}),rows=all.filter(isProcessRow);ignored+=all.length-rows.length;if(rows.length)items.push({name,rows});}if(!items.length)throw Error("Nenhuma aba com processos válidos foi encontrada.");available=detectCols(items);master=mergeSheets(items);clearAssignments();buildGroups();filtered=[...master];activeFilter="";selected=DEFAULT_COLS.filter(c=>available.includes(c));if(!selected.includes("Sorteado Para"))selected.push("Sorteado Para");batchDone=false;colBtn.disabled=previewBtn.disabled=false;packageBtn.disabled=true;render(filtered);summaryUI(filtered);modal("Planilha carregada",`<p><strong>${master.length}</strong> processos únicos válidos foram identificados.</p><p>${ignored} linha(s) auxiliares/resumos foram ignoradas.</p><p>A coluna <strong>Sorteado Para</strong> foi iniciada vazia e os nomes antigos de Sócio a gerir não serão usados.</p>`);}catch(err){modal("Erro ao ler planilha",`<p>${err.message}</p>`);reset();}};reader.readAsArrayBuffer(file);};

  colBtn.onclick=()=>{if(!available.length)return;const numbered=available.map((c,i)=>`${i+1}. ${c}${REQUIRED.includes(c)?" [essencial]":c==="Sócio a gerir"?" [origem]":""}`).join("\n"),defaults=selected.map(c=>available.indexOf(c)+1).filter(n=>n>0).join(","),ans=prompt(`Escolha as colunas que aparecerão na tela e nos relatórios.\n\nDigite os números separados por vírgula.\n\n${numbered}`,defaults);if(ans===null)return;const nums=ans.split(/[,;\s]+/).map(Number).filter(n=>Number.isInteger(n)&&n>=1&&n<=available.length);selected=available.filter((c,i)=>nums.includes(i+1)||REQUIRED.includes(c));if(!selected.includes("Sorteado Para"))selected.push("Sorteado Para");render(filtered);modal("Colunas selecionadas",`<p>${selected.length} colunas serão exibidas nos relatórios.</p>`);};

  const labels={EF:"EF",IMPROCEDENTE:"Improcedente",ED:"ED",EP:"EP",ALEATORIO:"Aleatório",NADJA:"NADJA/Ana","NADJA/FLAVIO":"NADJA/Flávio",COMPROMETIDO:"Comprometido",FLAVIO:"Cliente Flávio",ANA:"Cliente Ana Paula"};
  function previewHtml(){let rows="";for(const g of [...RANDOM_GROUPS,...FIXED_GROUPS]){const data=groupRows(g),rule=RANDOM_GROUPS.includes(g)?"60/40":(["ANA","NADJA"].includes(g)?"Ana":"Flávio");const q=RANDOM_GROUPS.includes(g)?quota(data):null;rows+=`<tr><td>${labels[g]}</td><td>${data.length}</td><td>${rule}</td><td>${q?`${q.flavio} Flávio / ${q.ana} Ana`:rule}</td></tr>`;}const grouped=[...groupMap.values()].reduce((a,b)=>a+b.length,0),outside=master.length-grouped;return`<table><thead><tr><th>Grupo</th><th>Qtd.</th><th>Regra</th><th>Resultado esperado</th></tr></thead><tbody>${rows}</tbody></table><p><strong>Processos válidos:</strong> ${master.length}<br><strong>Classificados:</strong> ${grouped}<br><strong>Fora dos grupos:</strong> ${outside}</p><p>O 60/40 é calculado separadamente em cada filtro. Um processo não entra em dois grupos.</p>`;}
  previewBtn.onclick=()=>modal("Conferência dos grupos",previewHtml());

  function executeBatch(){clearAssignments();assignFixed();RANDOM_GROUPS.forEach(assignRandom);batchDone=true;packageBtn.disabled=false;filtered=[...master];activeFilter="";render(filtered);summaryUI(master);modal("Sorteios concluídos",`${previewHtml()}<p>Os resultados foram gravados exclusivamente na coluna <strong>Sorteado Para</strong>.</p>`);}
  $("drawBtn").onclick=()=>{if(!master.length)return modal("Atenção","<p>Selecione a planilha primeiro.</p>");modal("Confirmar sorteios",`${previewHtml()}<p><strong>Confirmar o processamento de todos os grupos?</strong></p>`,{confirmText:"Executar sorteios",onConfirm:executeBatch});};

  function render(data){const hr=$("headerRow"),tb=$("tableBody");hr.innerHTML=tb.innerHTML="";if(!data.length)return;const cols=selected.filter(c=>available.includes(c)||c==="Sorteado Para");const sh=document.createElement("th"),sa=document.createElement("input");sa.type="checkbox";sh.appendChild(sa);hr.appendChild(sh);cols.forEach(c=>{const th=document.createElement("th");th.textContent=c;hr.appendChild(th);});data.forEach((r,i)=>{const tr=document.createElement("tr"),sd=document.createElement("td"),cb=document.createElement("input");cb.type="checkbox";cb.className="rowCheckbox";cb.dataset.index=i;sd.appendChild(cb);tr.appendChild(sd);cols.forEach(c=>{const td=document.createElement("td");td.textContent=c==="Valor da causa"?brl(r[c]):(r[c]??"");tr.appendChild(td);});tb.appendChild(tr);});sa.onchange=()=>document.querySelectorAll(".rowCheckbox").forEach(c=>c.checked=sa.checked);}
  function sum(data){let ac=0,av=0,fc=0,fv=0;data.forEach(r=>{const p=partner(r),v=parseBRL(r["Valor da causa"]);if(p==="Ana"){ac++;av+=v;}else if(p==="Flávio"){fc++;fv+=v;}});return{ac,av,fc,fv,tc:ac+fc,tv:av+fv};}
  function summaryUI(data){const s=sum(data);$("qtdAna").textContent=s.ac;$("percentAna").textContent=pct(s.ac,s.tc);$("valorAna").textContent=brl(s.av);$("percentValorAna").textContent=pct(s.av,s.tv);$("qtdFlavio").textContent=s.fc;$("percentFlavio").textContent=pct(s.fc,s.tc);$("valorFlavio").textContent=brl(s.fv);$("percentValorFlavio").textContent=pct(s.fv,s.tv);$("qtdTotal").textContent=s.tc;$("valorTotal").textContent=brl(s.tv);}

  function applyFilter(){const f=norm($("filterInput").value),map={EP:"EP",ED:"ED",EF:"EF",IMPROCEDENTE:"IMPROCEDENTE",ALEATORIO:"ALEATORIO",NADJA:"NADJA","NADJA/FLAVIO":"NADJA/FLAVIO",COMPROMETIDO:"COMPROMETIDO",FLAVIO:"FLAVIO",ANA:"ANA"};if(!map[f])return modal("Filtro inválido","<p>Selecione um filtro válido.</p>");activeFilter=map[f];filtered=groupRows(activeFilter);render(filtered);summaryUI(filtered);}
  $("applyFilterBtn").onclick=applyFilter;$("clearFiltersBtn").onclick=()=>{$("searchInput").value=$("filterInput").value="";activeFilter="";filtered=[...master];render(filtered);summaryUI(filtered);};$("searchInput").oninput=()=>{const t=norm($("searchInput").value);activeFilter="";filtered=master.filter(r=>Object.entries(r).some(([k,v])=>!k.startsWith("__")&&norm(v).includes(t)));render(filtered);summaryUI(filtered);};$("sortAZ").onclick=()=>{filtered.sort((a,b)=>String(a.Cliente||"").localeCompare(String(b.Cliente||""),"pt-BR"));render(filtered);};$("sortZA").onclick=()=>{filtered.sort((a,b)=>String(b.Cliente||"").localeCompare(String(a.Cliente||""),"pt-BR"));render(filtered);};$("selectAllBtn").onclick=()=>document.querySelectorAll(".rowCheckbox").forEach(c=>c.checked=true);$("deleteSelBtn").onclick=()=>{const idx=[...document.querySelectorAll(".rowCheckbox:checked")].map(c=>+c.dataset.index).sort((a,b)=>b-a);for(const i of idx){const k=key(filtered[i]);master=master.filter(r=>key(r)!==k);filtered.splice(i,1);}buildGroups();render(filtered);summaryUI(filtered);};

  const reportCols=()=>selected.filter(c=>available.includes(c)||c==="Sorteado Para");
  function makePDF(title,rows){const doc=new jspdf.jsPDF({orientation:"landscape"}),cols=reportCols();doc.setFontSize(13);doc.text(title,14,12);doc.setFontSize(8);doc.text(`Exportado em: ${new Date().toLocaleString("pt-BR")}`,14,18);doc.autoTable({startY:22,head:[cols],body:rows.map(r=>cols.map(c=>c==="Valor da causa"?brl(r[c]):r[c]??"")),styles:{fontSize:6,cellPadding:1.1}});const s=sum(rows),y=doc.lastAutoTable.finalY+7;doc.text("Resumo",14,y);doc.autoTable({startY:y+3,head:[["Sócio","Qtd","% Qtde","Valor R$","% Valor"]],body:[["Ana",s.ac,pct(s.ac,s.tc),brl(s.av),pct(s.av,s.tv)],["Flávio",s.fc,pct(s.fc,s.tc),brl(s.fv),pct(s.fv,s.tv)],["Total",s.tc,"100%",brl(s.tv),"100%"]],styles:{fontSize:8}});return doc;}
  $("exportPDF").onclick=()=>{if(!filtered.length)return modal("Sem dados","<p>Nenhum dado para exportar.</p>");makePDF(activeFilter?`Relatório - ${labels[activeFilter]||activeFilter}`:"Relatório de Processos",filtered).save("relatorio-processos.pdf");};$("exportXLSX").onclick=()=>{if(!filtered.length)return modal("Sem dados","<p>Nenhum dado para exportar.</p>");const cols=reportCols(),rows=filtered.map(r=>Object.fromEntries(cols.map(c=>[c,r[c]??""]))),wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows,{header:cols}),"Dados");XLSX.writeFile(wb,"relatorio-processos.xlsx");};
  async function loadJSZip(){if(window.JSZip)return;await new Promise((resolve,reject)=>{const s=document.createElement("script");s.src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js";s.onload=resolve;s.onerror=()=>reject(Error("Falha ao carregar JSZip"));document.head.appendChild(s);});}
  packageBtn.onclick=async()=>{if(!batchDone)return modal("Atenção","<p>Execute os sorteios do lote primeiro.</p>");try{await loadJSZip();const zip=new JSZip(),reports=[["EP","EP"],["ED","ED"],["EF","EF"],["IMPROCEDENTE","IMPROCEDENTE"],["ALEATORIO","ALEATORIO SORTEADO"],["NADJA","NADJA-ANA"],["NADJA/FLAVIO","NADJA-FLAVIO"],["COMPROMETIDO","COMPROMETIDO FLAVIO"],["FLAVIO","CLIENTES FLAVIO"],["ANA","CLIENTE ANA PAULA"]];for(const [g,n] of reports){const rows=groupRows(g);if(rows.length)zip.file(`${n}.pdf`,makePDF(n,rows).output("arraybuffer"));}zip.file("RESUMO CONSOLIDADO.pdf",makePDF("Resumo Consolidado",master.filter(r=>partner(r))).output("arraybuffer"));const blob=await zip.generateAsync({type:"blob"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`Relatorios_Sorteio_${new Date().toISOString().slice(0,10)}.zip`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);}catch(err){modal("Erro ao gerar relatórios",`<p>${err.message}</p>`);}};
  function reset(){master=[];filtered=[];available=[];selected=[...DEFAULT_COLS];activeFilter="";batchDone=false;groupMap=new Map();$("excelFile").value=$("searchInput").value=$("filterInput").value="";$("headerRow").innerHTML=$("tableBody").innerHTML="";colBtn.disabled=previewBtn.disabled=packageBtn.disabled=true;summaryUI([]);}
  $("clearBtn").onclick=reset;
});
